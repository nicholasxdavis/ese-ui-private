{
  "name": "el-sombrero-express",
  "private": true,
  "scripts": {
    "scrape": "node scripts/run-scrape.mjs",
    "serve": "node server.js",
    "start": "npm run scrape && npm run serve",
    "dev": "npx wrangler@latest dev",
    "deploy": "npx wrangler@latest deploy",
    "seed": "node scripts/seed-kv.mjs",
    "cf:whoami": "npx wrangler@latest whoami"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.2.0"
  }
}
