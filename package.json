{
  "name": "el-sombrero-express",
  "private": true,
  "scripts": {
    "scrape": "node scripts/run-scrape.mjs",
    "serve": "node server.js",
    "start": "npm run scrape && npm run serve",
    "dev": "node scripts/prepare-assets.mjs && wrangler dev",
    "deploy": "node scripts/prepare-assets.mjs && wrangler deploy",
    "prepare:assets": "node scripts/prepare-assets.mjs",
    "seed": "node scripts/seed-kv.mjs",
    "smoke": "node scripts/smoke-prod.mjs",
    "cf:whoami": "wrangler whoami"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "multer": "^2.2.0"
  }
}
